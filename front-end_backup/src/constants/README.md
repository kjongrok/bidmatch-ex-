# Constants

화면과 API에서 공통으로 사용하는 상수를 보관합니다.

## 예정 파일

- `routes.js`: 라우트 경로 상수
- `status.js`: 공고/알림/수집 상태 값
- `filters.js`: 업무 구분, 물품분류, 지역 등 필터 옵션

## 작성 기준

- 하드코딩된 문자열이 여러 파일에 반복되면 이 폴더로 이동합니다.
- 백엔드 enum 또는 코드 테이블과 값이 맞는지 확인합니다.
